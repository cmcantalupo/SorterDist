template <class T>
void SorterThread::sort(std::vector<T>::iterator begin, 
                        std::vector<T>::iterator end) {

  // Pick number of thread pivots
  it = begin + numThread;

  std::set<T> pivots(begin, it)
  
  // Partition the array
  
  // 
}



